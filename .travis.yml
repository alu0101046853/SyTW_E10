language: node_js
node_js:
          - "15"
dist: trusty
sudo: false

#branches:
cache:
  directories:
  - node_modules

install:
  - npm install -g yarn

script: 
  - yarn lint

on: 
  branch: edgar
